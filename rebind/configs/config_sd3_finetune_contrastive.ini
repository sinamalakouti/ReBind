[eval]
metrics =["vqascore"]
evaluator_model = vqascore_clip-flant5-xxl

[model]

base_t2i = dall-e-3
llm_model = gpt-4o


[dpo]
beta_dpo = 5000


[data]
num_examples_per_intermediate ="full" # 1, 3, 5,10,20
num_intermediates_per_relation ="full" # full: use whaterver is there (there are could be different number of intermediates for different relations, or maybe diff passive/active)
intermediate_types = ["active", "passive"] # active or passive